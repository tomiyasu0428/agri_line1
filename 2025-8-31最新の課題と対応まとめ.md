# 直進アシストアプリ｜進捗・課題・対策まとめ

---

## 🎯 1. プロジェクト概要と目標

### 目的
高価な専用機材を使わずに、スマートフォンと外部GNSSレシーバー（Garmin GLO 2など）を用いて、農作業における高精度な直進走行を支援する。

### 目標（Acceptance Criteria）
-   **精度**: Android + GLO 2環境で、直進100m走行時の平均横誤差を **±0.25m以内** に抑える（スマホGPS比50%以上改善）。
-   **安定性**: 屋外で連続5分間、GNSSの更新レートを **実効9Hz以上** で維持し、データ欠測率を2%以下に抑える。
-   **操作性**: 停止中に「ゼロ」ボタンをタップするだけで、95%以上の再現性で横方向のズレをリセットできる。

---

## 📊 2. 現在のステータス (到達点)

-   **プロトタイプ**: スマートフォン単体のGPSで、ABライン設定・進行方位表示・左右へのガイダンス表示の基本機能が動作。
-   **精度に関する知見**:
    -   長いABラインを設定するほど、ガイダンスが安定することを確認。
    -   「ゼロ（オフセットリセット）」機能が、特定の条件下で期待通りに動作しない問題を発見。
-   **技術方針**:
    -   **Android**: Bluetooth (SPP) 経由でGarmin GLO 2を接続し、NMEAセンテンスを直接読み取ることで **10Hz** の高更新レートを実現する方針を決定。
    -   **iOS**: Core Locationフレームワークの制約（1-3Hz）を前提とし、データスムージングや速度に応じた方位推定でUI/UXを最適化する方針。

---

## 🐛 3. 主要な課題と対策

| 課題 (Problem) | 影響 (Impact) | 対策 (Solution) | 優先度 |
| :--- | :--- | :--- | :--- |
| **スマホGPSの更新レート不足 (1-3Hz)** | 直進ガイドがふらつき、信頼性が低い | **AndroidでGLO 2を高レート(10Hz)で直接利用** | ★★★★☆ |
| **「ゼロ」リセットが効かない** | 作業再開時に中心線がズレる | **停止状態の検知を厳密化**し、オフセット適用のロジックを修正する | ★★★★☆ |

---

## 🛠️ 4. 実装方針と技術詳細

### 4.1. GNSSソースの抽象化
-   **方針**: `GnssSource` という共通インターフェースを設け、その実装をプラットフォームやデバイスに応じて切り替える。
    -   `AndroidNmeaSource`: Android + GLO 2 (10Hz) のための実装 (SPP接続/NMEA直読)
    -   `SystemLocationSource`: 標準のGPS機能を利用する実装 (Android/iOS共通, 1-3Hz)
-   **利点**: この設計により、Android側のNMEA関連の実装がiOSのコードに影響を与えることを防ぐ。

---

## 🚀 5. 次のアクション

### 実装タスク
1.  **AndroidNmeaSourceの実装**: GLO 2とのSPP接続、NMEAパーサー、10Hzでのデータフローを確立する。
4.  **フォアグラウンドサービス化**: アプリがバックグラウンドに回っても接続が途切れないようにする。

### テストタスク
1.  **ログ機能**: 受信したNMEAデータを保存し、再生(リプレイ)できるデバッグ機能を実装する。
2.  **フィールドテスト**:
    -   **安定性**: 屋外で実効9-10Hzが維持できるか確認。
    -   **再現性**: 「停止→ゼロ→次ラインへ進入」のシナリオを繰り返しテスト。
    -   **低速走行**: 0.5-1.0 m/sでのガイダンス安定性を確認。

---

## 📝 6. 補足：プラットフォーム別考慮点

### Android
-   **権限**: `BLUETOOTH_CONNECT`, `BLUETOOTH_SCAN`, `ACCESS_FINE_LOCATION` が必要。
-   **前提**: 事前にOSのBluetooth設定でGLO 2をペアリングしておく必要がある。

### iOS
-   **制約**: NMEA直接アクセスは不可。Core Location経由の1-3Hz更新が前提。
-   **品質評価**: HDOPが取得できないため、`horizontalAccuracy` (水平精度) と方位の安定性で品質を代替評価する。
-   **実装の分離**: Android専用のBluetoothコードがiOSのビルドターゲットに含まれないように注意が必要。